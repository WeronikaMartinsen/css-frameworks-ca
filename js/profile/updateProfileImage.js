/* export async function updateProfileImage(profileId) {
  try {
    const imageURL = document.getElementById("avatarInput").value;

    const updatedProfile = await sendUpdateRequest(profileId, imageURL);
    console.log("Profile image updated successfully:", updatedProfile);

    document.getElementById("profileImage").src = imageURL;
  } catch (error) {
    console.error("Error updating profile image:", error);
  }
}
 */
